//  Implement the sort function to sort an array of numbers
// First try it with a bubble sort
// Will need a swapped/empty variable and a while loop (maybe do/while)

function sort(arr) {
  let swapped;
  do {} while (swapped);
  return arr;
}

sort([]); // []
sort([-4, 1, Infinity, 3, 3, 0]); // [-4, 0, 1, 3, 3, Infinity]
